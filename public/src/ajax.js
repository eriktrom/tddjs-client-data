// Generated by CoffeeScript 1.6.1

(function() {
  var ajax, option, options, xhr, _i, _len;
  xhr = null;
  ajax = tddjs.namespace("ajax");
  options = [
    function() {
      return new ActiveXObject("Microsoft.XMLHTTP");
    }, function() {
      return new XMLHttpRequest();
    }
  ];
  for (_i = 0, _len = options.length; _i < _len; _i++) {
    option = options[_i];
    try {
      xhr = option();
      if (typeof xhr.readyState === "number" && tddjs.isHostMethod(xhr, "open") && tddjs.isHostMethod(xhr, "send") && tddjs.isHostMethod(xhr, "setRequestHeader")) {
        ajax.create = option;
        break;
      }
    } catch (e) {

    }
  }
})();
